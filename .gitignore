# Build outputs
build/
build-web/

# Downloaded build tools (sokol-shdc, fetched on demand by `make shaders`).
.tools/

# Editor/IDE
.vscode/
.idea/
*.swp
*~

# macOS / Windows cruft
.DS_Store
Thumbs.db

# Local Claude config (per-user, not committed)
.claude/settings.local.json
