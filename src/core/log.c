/* log.c — placeholder so the Makefile has a stable .c per header.
 * The macros in log.h are header-only; this TU exists to keep the
 * "one .c per module" convention consistent and gives us a place to
 * grow file/level logging later without breaking includes. */
#include "log.h"
